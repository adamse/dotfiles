[general]
ui = TTY.TTYUI
accounts = adamse
pythonfile=~/.mutt/offlineimap.py
fsync = False

[Account adamse]
localrepository = adamse-local
remoterepository = adamse-remote
status_backend = sqlite
postsynchook = notmuch new

[Repository adamse-local]
type = Maildir
localfolders = ~/.mail/adamse

[Repository adamse-remote]
maxconnections = 1
type = IMAP
remotehost = mail.messagingengine.com
remoteport = 993
remoteuser = adamse@fastmail.fm
remotepasseval = get_keychain_pass(account="adamse@fastmail.fm", server="mail.messageengine.com")
realdelete = no
nametrans = lambda folder: {'[Gmail]/Drafts':    'drafts',
                            '[Gmail]/Sent Mail': 'sent',
                            '[Gmail]/Starred':   'flagged',
                            '[Gmail]/Trash':     'trash',
                            '[Gmail]/All Mail':  'archive',
                            '[Gmail]/Spam':      'spam',
                            }.get(folder, folder)
folderfilter = lambda folder: folder not in []
